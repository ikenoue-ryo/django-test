language: python

python:
    - "3.4"
    - "3.5"
    - "3.6"

env:
  - DJANGO_VERSION=1.11
  - DJANGO_VERSION=1.10

install:
    - pip install django==$DJANGO_VERSION

script:
    - python -Wall setup.py test